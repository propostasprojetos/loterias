services:
  pocketbase:
    image: ghcr.io/muchobien/pocketbase:latest
    container_name: lotosmart-db
    restart: unless-stopped
    ports:
      - "8090:8090"
    volumes:
      - ./pb_data:/pb_data
      - ./pb_public:/pb_public
    command: --http=0.0.0.0:8090
